#!/usr/bin/node
/**
 * Prints 3 lines: (like 1-multi_languages.js) but by using an array of string
 */

const lines = ["C is fun", "Python is cool", "JavaScript is amazing"];

for (let i = 0; i < lines.length; i++) {
  console.log(lines[i]);
}
